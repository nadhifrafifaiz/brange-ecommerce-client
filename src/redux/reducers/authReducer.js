/* eslint-disable no-alert, no-console */
const initialState = {
  userId: "",
  username: "tes",
  email: "",
  roleId: "",
  isLogin: false,
};

const auth = (state = initialState, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default auth;
